FEATURES {
	STARTADDRESS: default = $0500;
}

MEMORY {
	MAIN:     file = %O, start = %S,    size = $07FF - %S;
}

SEGMENTS {
	CODE:     load = MAIN,     type = rw;
	DATA:	  load = MAIN,     type = rw, optional = yes;
}
