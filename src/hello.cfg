FEATURES {
	STARTADDRESS: default = $8000;
}

MEMORY {
	LOADADDR: file = %O, start = $0000, size = $0002;
	MAIN:     file = %O, start = %S,    size = $89fE - %S;
}

SEGMENTS {
	LOADADDR: 		load = LOADADDR, 	type = ro;
	CODE: 			load = MAIN,    	type = rw;
	TITLE_PAGE:	  	load = MAIN,     	type = rw;
	CODE2: 			load = MAIN,    	type = rw;
	BOOTSTRAP_1541:	load = MAIN,		type = rw;
	CODE3:			load = MAIN,		type = rw;
	FASTLOAD_1541:	load = MAIN,		type = rw;
	CODE4:			load = MAIN,		type = rw;
}