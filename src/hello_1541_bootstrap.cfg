FEATURES {
	STARTADDRESS: default = $0146;
}

MEMORY {
	MAIN:     file = %O, start = %S,    size = $01FF - %S;
}

SEGMENTS {
	CODE:     load = MAIN,     type = rw;
	DATA:	  load = MAIN,     type = rw, optional = yes;
}
